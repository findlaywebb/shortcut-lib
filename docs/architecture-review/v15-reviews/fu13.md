# Review: fu13 — `TextSplit.Show-text` toggle

**Branch:** `v15/fu13-textsplit-showtext` (head: f1d9e2d)
**Date:** 2026-05-09
**Verdict:** GREEN

---

## Test result

```
tests/test_action_text_split.py   9 passed
tests/test_wire_format_equivalence.py  29 passed, 6 skipped, 2 xfailed
Full suite: 333 passed, 6 skipped, 2 xfailed, 8 warnings
```

xfail count dropped from 3 (on main) to 2 — exactly as the brief required.
`test_text_split_wire_format` is now passing. RepeatCount and ChooseFromMenu
remain xfailed and untouched.

---

## What landed

Three files changed, 37 insertions, 21 deletions:

- **`src/shortcut_lib/schema/actions/text_split.py`** — `show_text: bool | None = None`
  field added; conditional `"Show-text"` emission in `_params()`; docstring
  updated.
- **`tests/test_action_text_split.py`** — two unit tests added:
  `test_text_split_show_text_emitted_when_set` and
  `test_text_split_show_text_omitted_by_default`.
- **`tests/test_wire_format_equivalence.py`** — `@pytest.mark.xfail` decorator
  and its accompanying bug-comment block stripped; test body updated to pass
  `show_text=True`; assertion converted to bare `assert schema_norm == sample_norm`.

`data/observed_envelope_types.json` — no diff. Verified with
`git diff main..v15/fu13-textsplit-showtext -- data/observed_envelope_types.json`.

---

## Assessment against the brief

| Criterion | Status | Notes |
|-----------|--------|-------|
| `show_text: bool \| None = None` field on `TextSplit` | PASS | Exact type and default |
| Emitted key is literal `"Show-text"` (hyphen, capital S) | PASS | Confirmed in source and live output |
| Field omitted from `_params()` when `self.show_text is None` | PASS | Guard is `if self.show_text is not None` |
| Docstring describes field as UI-only, no runtime semantics, analogous to `DownloadURL.ShowHeaders` | PASS | Wording matches; "UI-only toggle", "No runtime semantic effect" |
| xfail decorator removed from `test_text_split_wire_format` | PASS | Stripped cleanly; section header comment updated too |
| Test body passes `show_text=True` | PASS | `TextSplit(input=reminders_var, separator="New Lines", show_text=True)` |
| `test_text_split_show_text_emitted_when_set` added | PASS | Present and passing |
| `test_text_split_show_text_omitted_by_default` added | PASS | Present and passing |
| `data/observed_envelope_types.json` byte-identical | PASS | Empty diff confirmed |
| `examples/control_flow_demo.py` emits cleanly | PASS | 4 shortcuts written without error |

---

## Issues

None blocking. A few observations worth noting:

**`show_text=False` explicit-False path** — verified manually. Passing
`show_text=False` correctly emits `"Show-text": False` in the wire output.
Only `None` triggers omission. The brief required this; no test covers it
explicitly, but both named tests adequately document the contract for the
True and default-None cases. Acceptable.

**Key ordering** — `Show-text` appears after `text` and before `UUID` in
the emitted dict (`['text', 'Show-text', 'UUID']`). This is the natural
append-order from `_params()`. The equivalence test uses `_normalise()` to
compare, so ordering does not affect correctness. No concern.

**Ruff pre-existing lint error in `builder.py`** — the only ruff finding
(`FURB110` in `builder.py:284`) is pre-existing on main and unrelated to
this change. The three changed files are lint-clean.

**`_normalise` helper** — the diff was checked; no changes were made to the
normalise helper. The existing helper handles the new `Show-text` key without
modification, as expected (it is not an envelope type).

**Agent did not explicitly document running `scripts/scan_envelope_types.py`**
— the JSON diff is empty, confirming the outcome is correct regardless.
Whether the agent ran the script or relied on the diff check is moot.

**`examples/control_flow_demo.py`** — untouched by the agent (correct).
The example uses `TextSplit` via `build_repeat_each`; its emit is unaffected
because `show_text` defaults to `None` and is therefore omitted.

---

## Merge recommendation

**Merge.** The implementation is a textbook opt-in boolean field: correct
type annotation, correct sentinel (`None` = omit), correct emission key,
correct guard. The xfail is properly retired, not bypassed. Both required
unit tests are present with adequate docstrings. The wire equivalence test
round-trips cleanly against the corpus sample. No unrelated files were
disturbed. Full suite green with no regressions.

## 2026-05-10 merge-readiness pass

**Verdict:** Pass

**Branch HEAD:** `f1d9e2d` (matches _SUMMARY.md record `f1d9e2d`)

**Merge against main:**
- Result: clean
- Conflict files: none
- Resolution: Auto-merge succeeded with no conflicts; `docs/known_identifiers.md` soft conflict did not arise (branch does not touch that file).

**Pytest on merged state:** 333 passed, 6 skipped, 2 xfailed, 8 warnings

**prek:** skipped — `pre-commit` not installed in worktree environment (binary not found); three changed files were ruff-clean per the original review.

**Drift / observations:**
- Branch is 28 commits behind main but the merge is fully clean — no conflicts in any of the 3 changed files.
- `show_text: bool | None = None` pattern is consistent with the established `DownloadURL.ShowHeaders` sibling on main; no drift.
- Test count on merged state (333) matches the original review's full-suite count exactly — no new tests from main-side branches affect this suite.
- No changes to `data/observed_envelope_types.json` or `docs/known_identifiers.md` from this branch; both are safe.

**Minor corrections applied:**
- none

**Concerns for higher-tier review:**
- none
